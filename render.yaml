services:
  - type: web
    name: chatbot
    env: python
    plan: free
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: python chatbot/chat.py
    autoDeploy: true
